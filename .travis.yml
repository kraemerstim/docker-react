language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t kraemerstim/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true kraemerstim/docker-react npm run test